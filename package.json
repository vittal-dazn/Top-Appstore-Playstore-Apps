{
  "name": "dazn-app-charts",
  "version": "1.0.0",
  "description": "Top 10 Sports apps on iOS App Store and Google Play, per DAZN market. Static site with build-time data generation.",
  "type": "module",
  "private": true,
  "scripts": {
    "fetch-data": "node scripts/fetch-data.mjs",
    "serve": "npx --yes serve public",
    "start": "npm run serve"
  },
  "engines": {
    "node": ">=18"
  },
  "devDependencies": {
    "google-play-scraper": "^10.0.0"
  }
}
